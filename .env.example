# =============================================================================
# Immich → Google Photos Sync - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# Generate APP_SECRET_KEY with: python3 generate_key.py

# -----------------------------------------------------------------------------
# Security (REQUIRED)
# -----------------------------------------------------------------------------
# Encryption key for API keys and tokens (at least 32 characters)
# Generate with: python3 generate_key.py
APP_SECRET_KEY=your-secret-key-for-encryption-at-least-32-chars

# -----------------------------------------------------------------------------
# Application URL (REQUIRED)
# -----------------------------------------------------------------------------
# Base URL where the application is accessible
# This MUST match your actual URL for OAuth redirects to work
# Examples:
#   http://localhost:8080           (local development)
#   http://192.168.1.100:8080       (local network)
#   https://sync.example.com        (production with domain)
BASE_URL=http://localhost:8080

# -----------------------------------------------------------------------------
# Google OAuth Credentials (REQUIRED for Google Photos sync)
# -----------------------------------------------------------------------------
# Get these from Google Cloud Console:
# 1. Go to https://console.cloud.google.com
# 2. Create/select project
# 3. Enable "Photos Library API"
# 4. Create OAuth 2.0 credentials (Web application)
# 5. Add redirect URI: {BASE_URL}/auth/google/callback
# 6. Copy Client ID and Secret here

GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# -----------------------------------------------------------------------------
# Database (Optional - defaults provided)
# -----------------------------------------------------------------------------
# Path to SQLite database file
DATABASE_PATH=/data/app.db

# -----------------------------------------------------------------------------
# Logging (Optional - defaults provided)
# -----------------------------------------------------------------------------
# Path to log files directory
LOG_PATH=/data/logs

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Notes
# -----------------------------------------------------------------------------
# - Immich URL and API key are configured via the web UI
# - All sensitive data is encrypted in the database
# - Keep this .env file secure and never commit it to version control
# - For production, use strong random values for APP_SECRET_KEY
# - Ensure BASE_URL matches your actual domain for OAuth to work

# Database Path
DATABASE_PATH=/data/app.db

# Log Settings
LOG_LEVEL=INFO
LOG_PATH=/data/logs
